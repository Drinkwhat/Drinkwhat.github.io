# CI/CD Pipeline

Automated testing, building, and deployment with GitHub Actions.

## Overview

Workflows in this repository handle testing, building, docs publishing, and releases:

```text
Code Push/PR
    ↓
┌───────────────────────────────────────┐
│   Workflow: CI (ci.yml)               │
│   ├─ Quality (ruff + bandit)          │
│   ├─ SAST (semgrep)                   │
│   ├─ Unit Tests (pytest)              │
│   ├─ Coverage Reports (codecov)       │
│   └─ Quality Scan (sonarqube)         │
└───────────────────────────────────────┘
    ↓ (push main/dev + tags + PR)
┌───────────────────────────────────────┐
│   Workflow: Docker Build (docker-build.yml) │
│   ├─ Build Image                      │
│   ├─ Tag Version                      │
│   └─ Push to ghcr.io                  │
└───────────────────────────────────────┘
┌───────────────────────────────────────┐
│   Workflow: Docs (docs.yml)           │
│   ├─ Build Sphinx docs                │
│   └─ Publish to personal Pages repo   │
└───────────────────────────────────────┘
    ↓ (on CI success)
┌───────────────────────────────────────┐
│   Workflow: Release (release.yml)     │
│   ├─ Bump version (dev)               │
│   └─ Tag + Release (main)             │
└───────────────────────────────────────┘
```

### Diagramma pipeline (Mermaid)

```mermaid
flowchart TD
  P[Push or PR on main or dev]
  M[Manual trigger]

  Q[CI quality]
  S[CI semgrep]
  T[CI test]
  A[CI autofix]

  B[Docker build]
  PRM[Docker promote]
  D[Deploy staging]

  DB[Docs build]
  DD[Docs deploy]

  R1[Release bump dev]
  R2[Release tag and release]

  SQ[SonarQube scan]

  P --> Q
  P --> S
  Q -->|needs| T
  M --> A

  P --> B
  B --> PRM
  T -->|CI success| PRM
  PRM -->|dev only + trivy ok| D

  P --> DB
  DB -->|main only| DD

  T -->|on CI success| R1
  T -->|on CI success| R2

  T --> SQ
```

## CI Workflow (`ci.yml`)

Runs on every push and pull request to `main` and `dev` (with `paths` filters).

### CI Jobs (current)

- **quality**: fast gate (`ruff check`, `ruff format --check`, `bandit`)
- **semgrep**: SAST scan (runs in parallel to `quality`/`test`)
- **secrets**: secrets scan in git history (Gitleaks) to prevent leaking API keys/tokens/certs
- **test**: unit tests + coverage + Codecov upload (needs `quality`)
- **sonarqube**: static analysis and quality gate scan (needs `test`)
- **autofix**: automatic on `dev` pushes (with loop prevention), runs on same-repo PRs, and available manually via `workflow_dispatch` (fork PRs are skipped)

### Why semgrep is not inside quality

`quality` is designed to fail fast in seconds. `semgrep` is kept separate because it can take longer and may depend on network/rule downloads. Keeping it separate improves throughput while still making the overall CI fail if semgrep fails.

### Why autofix is limited on PRs

Autofix includes loop prevention and can push commits. For security, it runs only on PRs coming from branches in the same repository (fork PRs are skipped).

### Autofix (current behavior)

- Runs automatically on `dev` pushes (skips commits whose message starts with `chore: autofix` to avoid loops)
- Requires PAT_TOKEN to be present as a repository secret to push commits that retrigger workflows

---

## Docker Build Workflow (`docker-build.yml`)

Builds Docker images, scans them, and publishes them to GHCR.

### Trigger events

- `pull_request` on `main`/`dev` (paths-filtered): build only (no push)
- `push` on `main`/`dev` (paths-filtered): build + push by SHA, then promote branch tag if CI is green
- `push` tag `v*`: build + push the version tag
- Workflow-only changes under `.github/workflows/**` can still trigger this workflow, but heavy jobs are skipped unless runtime/deploy files changed (see `changes` job in `docker-build.yml`)

### Jobs (current)

- **build** (self-hosted docker-heavy):
  - builds the image
  - pushes `:sha-<shortsha>` on branch pushes
  - runs Trivy scan
- **promote** (GitHub-hosted):
  - waits for CI checks on the same commit
  - if CI is green, retags `:dev`/`:main` to the corresponding `:sha-<shortsha>` without rebuilding
  - runs locally on the same machine as staging (no Tailscale/SSH hop)
  - **Automated Cleanup**: Executes `docker image prune` after each deployment to keep the server disk healthy (removes unused images older than 7 days).
  - **Reliability**: The deploy is triggered if CI and build are green, ensuring staging is updated even if minor security warnings are found (while main remains strictly protected).
  - **Sentry Integration**: Notifies Sentry of new releases using the commit SHA.

---

## Runner selection (self-hosted + GitHub-hosted)

Workflows select runners via repository variables:


If variables are not set, workflows fall back to `["ubuntu-latest"]`.

For the production VM deploy, set both `PROD_GHCR_USERNAME` and `PROD_GHCR_TOKEN`.
The token must be a PAT with `read:packages` access and must be allowed to read the
`ghcr.io/<owner>/<repo>` package published by the repository. If the package is private
and the token cannot read it, Docker will fail on pull with `unauthorized`.
---

## Execution Flow

### Pull Request

```text
PR opened/updated
  ↓
CI runs (quality + semgrep + test)
  ↓
Docker Build runs (build only, no push)
  ↓
All required checks green → merge allowed
```

### Main Branch Push

```text
Merge to main
  ↓
CI runs and must be green
  ↓
Docker Build pushes sha-tag and promotes main-tag after CI success
  ↓
Release workflow tags and publishes a GitHub release (main only)
```

### Version Tag

```text
Release / tag-and-release exports openapi.json
  ↓
Create tag vX.Y.Z (only after the export succeeds)
  ↓
Docker Build builds and pushes vX.Y.Z image tag
  ↓
Release / tag-and-release publishes the GitHub Release for vX.Y.Z,
attaching the pre-exported openapi.json
```

### OpenAPI schema export (`release.yml` – `tag-and-release`)

Before tagging, the pipeline runs `scripts/export_openapi.py` and holds onto
the resulting `openapi.json`, later attaching it as a release asset. The
export step runs *before* the tag is created and pushed on purpose: if it
fails, the job stops before anything irreversible happens, instead of
leaving a dangling git tag with no matching GitHub Release. The script
imports `app.main` and calls `app.openapi()` directly — it does not scrape a
running server — so it works in CI without a live database, Redis, or
Keycloak instance; missing required settings are filled with inert
placeholders (see the script's module docstring).

This gives downstream consumers (e.g. a generated client SDK) a pinned,
versioned schema snapshot per release, instead of relying on the live
`/openapi.json` endpoint — only exposed in non-production environments
(see `docs/04-api-reference.md`, OWASP API9 default-deny). Run
`make export-openapi` locally to produce the same file for ad-hoc
inspection.

---

## Status Checks

### Required Checks (main branch)

Add to branch protection rules:

- ✅ CI / quality
- ✅ CI / semgrep
- ✅ CI / test
- ✅ Docker Build / build
- ✅ Docs / build-docs

### View Status

```text
GitHub UI:
  Settings → Branches → Branch protection rules

Pull Request:
  Shows check status: ✅ or ❌
```

---

## Debugging

### View Workflow Runs

```text
GitHub UI:
  Actions tab → Workflow runs
```

### Check Logs

Click on workflow run → Click on job → View logs

### Common issues

- Quality fails: run `ruff check` and `ruff format` locally
- Semgrep fails: check network/rules download; retry on GitHub-hosted
- Docker build fails on self-hosted: verify Docker daemon access and disk space

---

## Environment Variables

### GitHub Secrets

The tables below list every secret and repository variable referenced across all workflows. Secrets are stored under **Settings → Secrets and variables → Actions → Secrets**; variables under the **Variables** tab.

#### `docker-build.yml` – Deploy (staging)

| Name | Type | Required | Description |
|---|---|---|---|
| `GITHUB_TOKEN` | Secret | Auto-provided | GHCR push/pull auth and API calls |
| `DISCORD_WEBHOOK_URL` | Secret | Optional | Discord deploy-notification webhook |
| `GOOGLE_API_KEY` | Secret | ✅ | Google Vision / Generative AI API key |
| `SERVICE_ACCOUNT_JSON_B64` | Secret | ✅ | Google service-account JSON (base64-encoded) |
| `DB_USER` | Secret | ✅ | PostgreSQL application user |
| `DB_PASSWORD` | Secret | ✅ | PostgreSQL application password |
| `DB_NAME` | Secret | ✅ | PostgreSQL application database name |
| `KEYCLOAK_DB_NAME` | Secret | ✅ | Keycloak PostgreSQL database name |
| `KEYCLOAK_DB_USER` | Secret | ✅ | Keycloak PostgreSQL user |
| `KEYCLOAK_DB_PASSWORD` | Secret | ✅ | Keycloak PostgreSQL password |
| `KEYCLOAK_ADMIN` | Secret | ✅ | Keycloak admin console username |
| `KEYCLOAK_ADMIN_PASSWORD` | Secret | ✅ | Keycloak admin console password |
| `KEYCLOAK_URL` | Secret | ✅ | Public Keycloak base URL |
| `PUBLIC_BASE_URL` | Secret | ✅ | Public base URL of the application |
| `CORS_ORIGINS` | Secret | ✅ | JSON list of allowed CORS origins |
| `CORS_ALLOW_CREDENTIALS` | Secret | ✅ | Whether to allow CORS credentials (`true`/`false`) |
| `SENTRY_DSN` | Secret | ✅ | Sentry connection string |
| `SENTRY_AUTH_TOKEN` | Secret | ✅ | Sentry API token (used for release creation) |
| `APITALLY_CLIENT_ID` | Secret | ✅ | Apitally API analytics client ID |

#### `docker-build.yml` – Repository variables

| Name | Type | Description |
|---|---|---|
| `ACTIONS_RUNNER_LABELS` | Variable | Default self-hosted runner label set |
| `ACTIONS_RUNNER_LABELS_GH` | Variable | Runner labels for lightweight/GitHub-hosted jobs |
| `ACTIONS_RUNNER_LABELS_PY` | Variable | Runner labels for Python jobs |
| `ACTIONS_RUNNER_LABELS_DOCKER` | Variable | Runner labels for Docker build jobs |
| `ACTIONS_RUNNER_LABELS_DEPLOY` | Variable | Runner labels for deploy jobs |
| `INTEGRATION_HOST` | Variable | Hostname/IP of the staging server |
| `SENTRY_ORG` | Variable | Sentry organization slug |
| `SENTRY_PROJECT` | Variable | Sentry project slug |

#### `ci.yml` – Continuous Integration

| Name | Type | Required | Description |
|---|---|---|---|
| `PAT_TOKEN` | Secret | Optional | Personal Access Token; falls back to `GITHUB_TOKEN` |
| `CODECOV_TOKEN` | Secret | Optional | Codecov upload token for coverage reports |
| `SONAR_TOKEN` | Secret | ✅ | Token for SonarQube analysis |
| `SONAR_HOST_URL` | Variable | Optional | URL of the SonarQube server (defaults to SonarCloud) |

#### `docs.yml` – Documentation publish

| Name | Type | Required | Description |
|---|---|---|---|
| `PERSONAL_PAGES_TOKEN` | Secret | ✅ | Token to push built docs to the pages repository |
| `PERSONAL_PAGES_REPO` | Secret | ✅ | Target repository for docs deployment |
| `PERSONAL_PAGES_BRANCH` | Secret | Optional | Target branch (defaults to `main`) |
| `PERSONAL_PAGES_DIR` | Secret | Optional | Target directory (defaults to `restrackit-core`) |
| `CODECOV_TOKEN` | Secret | Optional | Codecov upload token embedded in the docs badge |

---

## Performance Optimization

### Caching

- uv cache: enabled via `astral-sh/setup-uv` and keyed on `uv.lock`
- Docker layer cache: Buildx uses `cache-from/to: type=gha`
- Trivy DB cache: persisted via `actions/cache` on `~/.cache/trivy`

### Parallel Jobs

CI jobs run in parallel where possible; Docker Build runs in parallel to CI but promotion/deploy are gated by CI success.

---

## Best Practices

### Branch Strategy

1. **Feature branches** → PR triggers CI only
2. **dev branch** → Passes CI → Docker build
3. **main branch** → Stable releases

### Semantic Versioning

Tag format: `v1.2.3`

- `1` = Major (breaking changes)
- `2` = Minor (features)
- `3` = Patch (fixes)

## Monitoring

### Notifications

- ✉️ Failed workflow email
- 🔔 GitHub notifications
- Slack integration (optional)

### Metrics

Track in CI/CD dashboard:

- Build time
- Test coverage
- Deployment frequency
- Failure rate

---

For local testing: See [Quick Reference](01-quick-reference.md)
